config EXTERNAL_FREETZ_PACKAGE_BINUTILS_TOOLS
	depends on EXTERNAL_ENABLED && FREETZ_PACKAGE_BINUTILS_TOOLS && EXTERNAL_SUBDIRS
	bool "binutils-tools (externalization recommended)"
	default n
	help
		Externalize binutils-tools binaries to USB storage.
		
		Recommended for devices with limited flash memory.
		The binutils-tools package can be stored on external storage
		to save internal flash space.
		
		Files externalized (only those selected):
		- /usr/bin/readelf
		- /usr/bin/objdump
		- /usr/bin/objcopy
		- /usr/bin/nm
		- /usr/bin/strings
		- /usr/bin/ar
		- /usr/bin/ranlib
		- /usr/bin/strip
		- /usr/bin/addr2line
		- /usr/bin/size