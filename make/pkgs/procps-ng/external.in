config EXTERNAL_FREETZ_PACKAGE_PROCPS_NG
	depends on EXTERNAL_ENABLED && FREETZ_PACKAGE_PROCPS_NG && EXTERNAL_SUBDIRS
	bool "procps-ng"
	default n
	help
		Externalize procps-ng binaries to USB storage.
		
		Recommended for devices with limited flash memory.
		The procps-full package includes multiple utilities
		that can be stored on external storage to save
		internal flash space.
		
		All selected binaries will be externalized with "full"
		suffix, eg "psfull".

