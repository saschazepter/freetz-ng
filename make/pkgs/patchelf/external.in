config EXTERNAL_FREETZ_PACKAGE_PATCHELF
	depends on EXTERNAL_ENABLED && FREETZ_PACKAGE_PATCHELF && EXTERNAL_SUBDIRS
	bool "patchelf (externalization recommended)"
	default n
	help
		Externalize patchelf binary to USB storage.
		
		Recommended for devices with limited flash memory.
		patchelf can be stored on external storage to save internal flash space.
		
		File externalized:
		- /usr/bin/patchelf
